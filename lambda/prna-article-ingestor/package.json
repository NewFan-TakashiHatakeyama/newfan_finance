{
  "name": "prna-article-ingestor",
  "version": "1.0.0",
  "description": "Lambda function to ingest PRNA articles from S3 to DynamoDB",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "deploy": "npm run build && sam deploy --guided",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.971.0",
    "@aws-sdk/client-dynamodb": "^3.971.0",
    "@aws-sdk/lib-dynamodb": "^3.971.0",
    "@upstash/redis": "^1.34.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20",
    "typescript": "^5",
    "rimraf": "^6.0.0"
  }
}
